spring:
  application:
    name: ecommerce-backend
  datasource:
    url: jdbc:mysql://mysql:3306/ecommerce_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

server:
  port: ${SERVER_PORT:8089}

app:
  jwt:
    secret: ${JWT_SECRET:ThisIsADevelopmentOnlySecretKeyForJwtGeneration123456789}
    expiration-ms: ${JWT_EXPIRATION_MS:86400000}
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:4200,http://localhost}

logging:
  level:
    root: INFO
    com.ecommerce.backend: DEBUG
